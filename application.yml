eureka:
  client:
    service-url:
      defaultZone: http://localhost:9999/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
info:
  author: Marcos Paulo Silva Spano
  mail: e.marcosspano@gmail.com
server:
  port: ${PORT:9999}
message: Generic
